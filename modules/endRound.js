const INDEX = require("../index.js")
var oneVone = []; //nested array containing players

const initRapBattle = () => {
    //TODO: pick 1v1 fighters + what to do about odd one out?
}

const nextPlayer = () => {

}

const cleanUp = () => {
    //TODO: delete all mp3s
    //TODO: reset userRoundData Object
}

const nextRound = () => {
    //TODO: check if end of game
    //TODO: initiate next Round
}

module.exports = {initRapBattle, nextPlayer, cleanUp, nextRound}